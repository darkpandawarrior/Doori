<!-- GENERATED by AgentHarness/scripts/capability-manifest.mjs — DO NOT EDIT BY HAND.
     Regenerate: node ~/Tools/DevTools/AgentHarness/scripts/capability-manifest.mjs .
     Durable knowledge that cannot be detected belongs in .capabilities-notes.md, which is
     spliced in verbatim below and IS hand-written. -->

# Capabilities — Doori

What is already wired here. Read this before deriving any of it yourself.

Repository: https://github.com/darkpandawarrior/Doori

## Targets

- Web (wasm)
- Desktop / JVM
- iOS
- watchOS
- Android

## See the UI

- `./gradlew :desktopApp:run` — Desktop window — fastest way to actually look at the shared UI
- `./gradlew :app-web-preview:wasmJsBrowserDevelopmentRun` — Browser — shared UI in a dev server
- `xcodebuild -project iosApp/iosApp.xcodeproj -scheme <scheme> -destination "id=$(xcrun simctl list devices available -j | jq -r '..|objects|select(.name?=="iPhone 17")|.udid' | head -1)" build` — iOS simulator — drive by UDID, never by name
- `open docs/screenshots` — 384 captured screenshots — look before you build

## Wired tooling

- **detekt** — static analysis
- **ktlint** — formatting
- **Kover** — coverage
- **Roborazzi** — screenshot / visual regression
- **dependency-guard** — locks the resolved dependency set
- **Compose Multiplatform** — shared UI
- **Koin** — DI
- **Ktor** — HTTP
- **Room** — local persistence

## Convention plugins in use

- `mileway.android.library`
- `mileway.kmp.desktop`
- `mileway.kmp.library.watchos`
- `shared.android.application`
- `shared.cmp.feature`
- `shared.kmp.compose`
- `shared.kmp.library`
- `shared.test`

Change one of these and every module applying it changes. That is usually the cheapest place to fix
something that appears in many modules at once.

## CI

- **Deploy to Amazon Appstore** (`amazon-appstore-deploy.yml`)
- **Deploy to Aptoide** (`aptoide-deploy.yml`)
- **Bump kmp-toolkit** (`bump-toolkit.yml`)
- **CI** (`ci.yml`)
- **Dependabot dependency-guard baseline** (`dependabot-rebaseline.yml`)
- **design-sentinel** (`design-sentinel.yml`)
- **GitHub Release** (`github-release.yml`)
- **Deploy to Huawei AppGallery** (`huawei-appgallery-deploy.yml`)
- **Deploy to Indus Appstore** (`indus-deploy.yml`)
- **iOS Build** (`ios.yml`)
- **No AI attribution** (`no-ai-attribution.yml`)
- **Publish F-Droid APK** (`publish-fdroid.yml`)
- **Quality Gate** (`quality.yml`)
- **README auto-refresh** (`readme.yml`)
- **Release** (`release.yml`)
- **Deploy to Samsung Galaxy Store** (`samsung-galaxy-store-deploy.yml`)
- **Screenshots** (`screenshots.yml`)

## Skills available in this repo

96 skills under `.claude/skills/`. Check for one covering your task before improvising:

`adaptive` · `adb-apps` · `adb-architecture` · `adb-automation` · `adb-capture` · `adb-control` · `adb-devices` · `adb-observability` · `adb-tests` · `adb-transfer` · `agp-9-upgrade` · `android-accessibility` · `android-bleeding-edge-modernization` · `android-cli` · `android-code-review` · `android-compose-ui` · `android-coroutines` · `android-data-layer` · `android-debugging` · `android-dev` · `android-di-koin` · `android-emulator-skill` · `android-error-handling` · `android-gradle-logic` · `android-localisation-translator` · `android-module-structure` · `android-navigation` · `android-performance-engineering` · `android-presentation-mvi` · `android-retrofit` · `android-source-search` · `android-tdd` · `android-test-concepts` · `android-test-doubles` · `android-test-espresso` · `android-test-managed-devices` · `android-test-runner` · `android-test-scenarios` · `android-test-strategies` · `android-test-uiautomator`

…and 56 more — `ls .claude/skills`

## Git

- hooks: `.githooks` — active. Never bypass with `--no-verify`; fix the cause.

## Hard-won notes

Hand-written. Spliced verbatim into `CAPABILITIES.md` by the manifest generator. Put anything here
that cost someone a debugging cycle and that no script can detect.

**xcodebuild: never pass `-sdk iphonesimulator`.** The `iosApp` scheme includes `MilewayWatch`, whose
`SDKROOT` is correctly `watchsimulator`. `-sdk` overrides SDKROOT for *every* target in the scheme and
produces a failure that looks like a project defect and is not. Use `-destination` alone.

**Drive xcodebuild by simulator UDID, never by name.** Two `iPhone 17` simulators exist (iOS 26.5 and
27.0), so `name=iPhone 17` is ambiguous and a generic destination asks for arm64 *and* x86_64 — a slice
the Kotlin framework does not have. Resolve the UDID first.

**AGP 9.5.0-alpha06 + Roborazzi collide.** AGP registers
`generate<Variant>ComposePreviewRunfiles` for every Compose-enabled variant and hard-fails when that
variant has no unit-test component. AGP itself drops it on library release variants; Roborazzi drops it
on all non-debug variants. The fix lives in the root `subprojects` block, gated on `com.android.base`,
because `beforeVariants` runs in *registration order* and Roborazzi registers after any convention
plugin — a convention-plugin fix silently does nothing here.

**Robolectric 4.17 needs `--add-exports=java.base/jdk.internal.access=ALL-UNNAMED`.** It reaches
`ApplicationSharedMemory.create`, whose `FileDescriptorInterceptor` touches `jdk.internal.access`. That
is an *internal* package, so `--add-opens` cannot fix it.

**Commit messages are screened for a leak-pattern list, matched case-insensitively on `dice`.** The word
*indices* contains it and will get your commit rejected. Write "positions" or "index values". Never
`--no-verify`; the hook is the point.

**`@Preview` in `commonMain` is `androidx.compose.ui.tooling.preview.Preview`.** That is the
multiplatform annotation from Compose Multiplatform 1.10.0 onward; `org.jetbrains.compose.ui.tooling.
preview.Preview` is the legacy path. Verified here: 7 `commonMain` files import the androidx FQN, zero
import the JetBrains one, and they compile.

**detekt baselines are per source set.** `./gradlew detektBaseline` writes *one* file; this repo has 30
and exposes 315 `detektBaseline*SourceSet` tasks. Deleting them all and running the single task destroys
29 of them and makes the finding count look like it collapsed.

**Never run a repo-wide Gradle task from an agent.** 930 tests take ~14 minutes and a long silent
command gets an agent killed for inactivity. Scope to the module and pass `--console=plain`.


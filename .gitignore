# Built application files / Gradle
*.apk
*.aar
*.ap_
*.aab
*.iml
.gradle/
build/
/build
/captures
.externalNativeBuild
.cxx

# Local config
local.properties
/local.properties

# Release signing secrets (never commit; see keystore.properties.template)
keystore.properties
*.jks
*.keystore

# IDE
/.idea
*.ipr
*.iws

# OS
.DS_Store

# Logs
*.log

# Agent tooling (local only)
.ralph/
.claude/
CLAUDE.md
AGENTS.md
.kotlin/

# versioned-release skill: local dry-run previews
.release/preview/
.release/scrublist.txt

# PLAN_V36 P8: scripts/whatsnew-draft.sh output — human-curated before landing, never committed
whatsnew-draft/

# Local git worktree checkouts (main repo or submodules) — machine-local, never committed
.worktrees/

# Code knowledge graph (graphify). Derived, rebuildable, and large; it also mirrors every
# symbol name in the repo, so it is never committed. Rebuild: node ~/Tools/DevTools/AgentHarness/scripts/graph.mjs build
graphify-out/

# Generated by scripts/build-screenshot-gallery.py — 4.4MB of embedded captures.
# Regenerate on demand; the PNGs it is built from are the tracked source of truth.
docs/screenshots/gallery.html
